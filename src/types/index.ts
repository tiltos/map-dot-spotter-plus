
export type PointOfInterestType = {
  id: string;
  name: string;
  description?: string;
  x: number;
  y: number;
  icon: string;
};

export type IconOption = {
  id: string;
  name: string;
  src: string;
};
